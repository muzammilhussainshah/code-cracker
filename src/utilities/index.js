// export { WP, HP } from './styling/responsive';
// export { colors } from './colors';
export {
    paginationLimit,
    DEFAULT_LANGUAGE,
    version,
    platform,
    systemVersion,
    deviceUId,
    deviceType,
    hasNotch
} from './constants';
export { languageData } from './languageData';
// export { loginValidation } from './validation';

// export const getTagsArray = (tags) => {
//     let tagArray;

//     if (tags && !Array.isArray(tags)) {
//         tagArray = [];
//         for (var key in tags) {
//             if (tagArray.indexOf(tags[key]) === -1) {
//                 tagArray.push(tags[key]);
//             }
//         }
//     } else {
//         tagArray = tags;
//     }

//     return tagArray;
// };
